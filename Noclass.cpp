#include <iostream>
using namespace std;
class No
{
private:
    int chave;
    No*prox;
public:
    No();
    No(int);
    int getChave();
    No* getprox();
    void getchave(int);
    void getprox(No* prox);
};
class Lista
{
private:
    No *inicio;
public:
    Lista();
    void insere_inicio(int)
};
No No
chave: O
prox:
3
No No (int chave)
chave-chave, prox = 0,
int No get Chave()
return chave
